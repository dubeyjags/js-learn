 // control flow | run the conditional or required code.

 const isUserValid = true;
 if(isUserValid){ // condition for comparision
    console.log("user valid");
 } else {
    console.log("user not valid");
 }

 // >,<,<=,>=,==,!=,!==,=== ,&& , ||
 // = use for assign
 // == for comparions
 //=== comparision with type

// && and || are the logical operator